
{
  "entities": {
    "Case": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Case",
      "type": "object",
      "description": "Represents a social case for displaced and vulnerable people.",
      "properties": {
        "id": { "type": "string" },
        "caseNumber": { "type": "string" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "documentId": { "type": "string" },
        "internalId": { "type": "string" },
        "ethnicGroup": { "type": "string" },
        "maritalStatus": { "type": "string" },
        "gender": { "type": "string" },
        "birthDate": { "type": "string", "format": "date-time" },
        "address": { "type": "string" },
        "locality": { "type": "string" },
        "municipality": { "type": "string" },
        "department": { "type": "string" },
        "phone1": { "type": "string" },
        "phone2": { "type": "string" },
        "displacementType": { "type": "string" },
        "disability": { "type": "string" },
        "age": { "type": "number" },
        "isElderly": { "type": "boolean" },
        "peopleCount": { "type": "number" },
        "testimony": { "type": "string" },
        "status": { "type": "string", "enum": ["Sin novedad", "CONTACTADO", "NO CONTACTADO"] },
        "userId": { "type": "string" },
        "members": { "type": "object" }
      },
      "required": ["id", "caseNumber", "firstName", "lastName", "documentId", "ethnicGroup", "maritalStatus", "gender", "birthDate", "address", "municipality", "department", "phone1", "displacementType", "age", "isElderly", "peopleCount", "testimony", "status"]
    },
    "Mensaje": {
      "title": "Mensaje",
      "type": "object",
      "description": "Public contact message received from the landing page.",
      "properties": {
        "nombre": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "cedula": { "type": "string" },
        "asunto": { "type": "string" },
        "mensaje": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "linkedCase": {
          "type": "object",
          "properties": {
            "id": { "type": "string" },
            "caseNumber": { "type": "string" }
          }
        }
      },
      "required": ["nombre", "email", "cedula", "mensaje", "createdAt"]
    },
    "User": {
      "title": "User",
      "type": "object",
      "description": "System user profile including roles.",
      "properties": {
        "id": { "type": "string" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["admin", "case-worker"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "email", "role"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "/cases/{caseId}": {
      "schema": "Case",
      "description": "Stores case data."
    },
    "/mensajes/{mensajeId}": {
      "schema": "Mensaje",
      "description": "Stores contact messages."
    },
    "/users/{userId}": {
      "schema": "User",
      "description": "Stores user profile and roles."
    }
  }
}
