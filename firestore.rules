
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============================================================
    // REGLAS DE SEGURIDAD RENACER - SISTEMA DE NOTIFICACIONES
    // PROPÓSITO: Permitir gestión de casos y notificaciones.
    // TIMESTAMP: 2024-05-20T21:30:00Z
    // ============================================================

    function isSignedIn() {
      return request.auth != null;
    }

    match /notifications/{notificationId} {
      allow read, update: if isSignedIn() && request.auth.uid == resource.data.userId;
      allow create: if isSignedIn();
    }

    match /{document=**} {
      allow read, write: if isSignedIn();
    }
  }
}
