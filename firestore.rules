rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas de Seguridad Renacer - Simplificadas al máximo para evitar errores de permisos
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
